# Prompt the user to input their current age
current_age = int(input("How old are you? "))

# Calculate the user's age in the year 2050
year_2050_age = current_age + 27  # Since the current year is assumed to be 2023

# Print the result
print(f"In 2050, you will be {year_2050_age} years old.")